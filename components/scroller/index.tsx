'use client'

// Scroller component is no longer needed for hash-based navigation
// Navigation is now handled directly through useNavigation hook and Zustand store
// ScrollTrigger in SectionSetter handles section detection during scroll

export function Scroller() {
  return null
}
